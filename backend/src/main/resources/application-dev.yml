spring:
  datasource:
    url: jdbc:h2:mem:qq_chat;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false

jwt:
  secret: ***REMOVED***

napcat:
  token: dev-token-napcat
  webhook-token: dev-webhook-token-for-napcat

minio:
  access-key: admin
  secret-key: password123
